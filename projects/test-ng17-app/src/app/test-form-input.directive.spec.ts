import { TestFormInputDirective } from './test-form-input.directive';

describe('TestFormInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TestFormInputDirective();
    expect(directive).toBeTruthy();
  });
});
